// animals.h
/***************************************************
 * int update_hare(int current_position)
 * Updates the position of the hare after each tick
 * Parameters:
 *      current_position: the current position of the hare
 * Returns:
 *      The new position of the hare
 * ************************************************/
int update_hare(int current_position);

/***************************************************
 * int update_tortoise(int current_position)
 * Updates the position of the tortoise after each tick
 * Parameters:
 *      current_position: the current position of the tortoise
 * Returns:
 *      The new position of the tortoise
 * ************************************************/
int update_tortoise(int current_position);

/***************************************************
 * int update_bee(int current_position)
 * Updates the position of the bee after each tick
 * Parameters:
 *      current_position: the current position of the bee
 * Returns:
 *      The new position of the bee
 * ************************************************/
int update_bee(int current_position);
